{
  "scripts": {
    "build": "npm install && node_modules/.bin/install-puppeteer-dependencies",
    "start": "node server.js"
    /{
  "scripts": {
    "build": "npm install && node_modules/.bin/install-puppeteer-dependencies",
    "start": "node server.js"
    // ... other scripts
  },
  "dependencies": {
    "puppeteer-core": "^22.14.0",
    "chrome-aws-lambda": "^10.0.0" // Consider using this helper package if on AWS Lambda or similar environments, though less common for Render.
    // ... your other dependencies
  }
}
/ ... other scripts
  },
  "dependencies": {
    "puppeteer-core": "^22.14.0",
    "chrome-aws-lambda": "^10.0.0" // Consider using this helper package if on AWS Lambda or similar environments, though less common for Render.
    // ... your other dependencies
  }
}
 
